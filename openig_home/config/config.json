{
    "heap": [
        {
            "name": "Router",
            "type": "Router",
            "config": {
                "directory": "/opt/openig/config/routes"
            }
        },
        {
            "name": "JwtSession",
            "type": "JwtSession",
            "config": {
                "cookieName": "IG_SSO",
                "sessionTimeout": "8 hours",
                "sharedSecret": "${env['JWT_SHARED_SECRET']}"
            }
        }
    ],
    "handler": "Router"
}
