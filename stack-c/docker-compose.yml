networks:
  frontend-c:
    driver: bridge
  backend-c:
    driver: bridge

volumes:
  redis_c_data:

services:
  nginx-c:
    image: nginx:1.25-alpine
    ports:
      - "18080:80"
    networks:
      - frontend-c
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - openig-c1
      - openig-c2

  openig-c1:
    image: openidentityplatform/openig
    entrypoint: ["/docker-entrypoint.sh"]
    networks:
      - frontend-c
      - backend-c
    volumes:
      - ./openig_home:/opt/openig
      - ./docker/openig/docker-entrypoint.sh:/docker-entrypoint.sh:ro
    environment:
      OPENIG_BASE: "/opt/openig"
      OPENIG_PUBLIC_URL: "http://openig-c.sso.local:10080"
      KEYCLOAK_BROWSER_URL: "http://auth.sso.local:8080"
      REDIS_HOST: "redis-c"
      JWT_SHARED_SECRET: "fThJvpugVMgfRGoRYBK2tnuFDAQ+7kdrN9u6romRcUM="
      KEYSTORE_PASSWORD: "Bx4oNKDVzSav7TMHK6gsKcLyhqCgspqm"
      OIDC_CLIENT_SECRET_APP5: "secret-c"
      OIDC_CLIENT_SECRET_APP6: "secret-c"
      VAULT_ADDR: "http://vault-c:8200"
      VAULT_ROLE_ID_FILE: "/opt/openig/vault/role_id"
      VAULT_SECRET_ID_FILE: "/opt/openig/vault/secret_id"
      CANONICAL_ORIGIN_APP5: "http://grafana-c.sso.local:18080"
      CANONICAL_ORIGIN_APP6: "http://phpmyadmin-c.sso.local:18080"

  openig-c2:
    image: openidentityplatform/openig
    entrypoint: ["/docker-entrypoint.sh"]
    networks:
      - frontend-c
      - backend-c
    volumes:
      - ./openig_home:/opt/openig
      - ./docker/openig/docker-entrypoint.sh:/docker-entrypoint.sh:ro
    environment:
      OPENIG_BASE: "/opt/openig"
      OPENIG_PUBLIC_URL: "http://openig-c.sso.local:10080"
      KEYCLOAK_BROWSER_URL: "http://auth.sso.local:8080"
      REDIS_HOST: "redis-c"
      JWT_SHARED_SECRET: "fThJvpugVMgfRGoRYBK2tnuFDAQ+7kdrN9u6romRcUM="
      KEYSTORE_PASSWORD: "Bx4oNKDVzSav7TMHK6gsKcLyhqCgspqm"
      OIDC_CLIENT_SECRET_APP5: "secret-c"
      OIDC_CLIENT_SECRET_APP6: "secret-c"
      VAULT_ADDR: "http://vault-c:8200"
      VAULT_ROLE_ID_FILE: "/opt/openig/vault/role_id"
      VAULT_SECRET_ID_FILE: "/opt/openig/vault/secret_id"
      CANONICAL_ORIGIN_APP5: "http://grafana-c.sso.local:18080"
      CANONICAL_ORIGIN_APP6: "http://phpmyadmin-c.sso.local:18080"

  grafana:
    image: grafana/grafana:latest
    networks:
      - backend-c
    environment:
      GF_AUTH_PROXY_ENABLED: "true"
      GF_AUTH_PROXY_HEADER_NAME: "X-WEBAUTH-USER"
      GF_AUTH_PROXY_HEADER_PROPERTY: "username"
      GF_AUTH_PROXY_AUTO_SIGN_UP: "true"
      GF_AUTH_DISABLE_LOGIN_FORM: "true"
      GF_SERVER_ROOT_URL: "http://grafana-c.sso.local:18080"

  mariadb:
    image: mariadb:11
    networks:
      - backend-c
    environment:
      MYSQL_ROOT_PASSWORD: "rootpass"
      MYSQL_DATABASE: "appdb"
      MYSQL_USER: "alice"
      MYSQL_PASSWORD: "AlicePass123"

  phpmyadmin:
    image: phpmyadmin:latest
    networks:
      - backend-c
    environment:
      PMA_HOST: "mariadb"
      PMA_AUTH_TYPE: "http"
    volumes:
      - ./openig_home/phpmyadmin/config.user.inc.php:/etc/phpmyadmin/config.user.inc.php:ro

  redis-c:
    image: redis:7-alpine
    command: redis-server --appendonly yes --appendfsync everysec
    volumes:
      - redis_c_data:/data
    networks:
      - backend-c
    restart: unless-stopped

  vault-c:
    image: hashicorp/vault:1.15
    networks:
      - backend-c
    cap_add:
      - IPC_LOCK
    command: server
    volumes:
      - ./vault/config:/vault/config:ro
      - ./vault/data:/vault/data
      - ./vault/file:/vault/file
      - ./vault/init:/vault/init
