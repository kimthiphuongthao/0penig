# Gotchas & Decision Log

## Known gaps (chưa xong)
- Stack C infra gap: phpMyAdmin path cho `bob` chưa support được vì live MariaDB chưa provision user `bob`

## Gotchas đã biết

| Vấn đề | Nguyên nhân | Fix |
|--------|-------------|-----|
| "SSO authentication failed" | openig-2 có file `role_id`/`secret_id` rỗng | Regenerate AppRole credentials → ghi vào shared mount |
| Vault "bind: address already in use" | `command: server -config=...` + entrypoint tự thêm `-config` → 2 listeners | Dùng `command: server` only |
| Bootstrap script abort silently | `vault status` exit 2 khi sealed + `set -euo pipefail` | `code=$(vault status >/dev/null 2>&1; echo $?)` + `|| true` |
| phpMyAdmin không nhận Basic Auth từ env | `PMA_AUTH_TYPE=http` env var không hoạt động với Docker image | Mount `config.user.inc.php` với `$cfg['Servers'][1]['auth_type'] = 'http'` |
| Jellyfin passwords không trong bootstrap | Jellyfin lưu passwords trong SQLite volume, bootstrap script không set được | Set thủ công qua Jellyfin API sau lần đầu. Nếu volume bị xóa phải set lại: alice/AliceJelly2026, bob/BobJelly2026 |
| TC-1201 false fail | Test check cookie trên unauthenticated request → chỉ thấy JSESSIONID | IG_SSO chỉ xuất hiện sau login. Không phải bug. |
| Keycloak hiện "Do you want to log out?" | `defaultLogoutGoto` tĩnh thiếu `id_token_hint` — Keycloak 18+ bắt buộc khi có `post_logout_redirect_uri` | Dùng custom SloHandler Groovy đọc `id_token` từ session, append `id_token_hint` động |
| Stack C backchannel logout không trigger | Route có Host condition match `grafana-c.sso.local` nhưng Keycloak POST tới `host.docker.internal` | Bỏ Host condition, chỉ match path `/openid/appX/backchannel_logout` |
| SloHandler URL `/null/realms/...` | `KEYCLOAK_BROWSER_URL` env var không set trong docker-compose Stack C | Thêm `KEYCLOAK_BROWSER_URL: "http://auth.sso.local:8080"` vào openig-c1 và openig-c2 |
| Grafana logout chỉ reload trang | Grafana `/logout` trả 302 (không phải 401) → không trigger failureHandler → không SLO | Thêm route `00-grafana-logout.json` intercept `GET /logout` → `SloHandler.groovy` (parameterized template) |
| Keycloak "Invalid parameter: id_token_hint" | SloHandler dùng `OIDC_CLIENT_ID` (shared) nhưng id_token được issue cho dedicated client (e.g. `openig-client-b-app4`) → client_id mismatch | Dùng env var riêng cho mỗi client (`OIDC_CLIENT_ID_APP4`), đảm bảo SloHandler dùng đúng client_id khớp với OAuth2ClientFilter |
| Keycloak "Invalid redirect uri" khi logout | `post.logout.redirect.uris` chưa configured trong Keycloak client dedicated (chỉ có trong shared client) | Thêm `post.logout.redirect.uris` vào Keycloak client dedicated qua admin API hoặc realm-export.json |
| Fail-closed redirect loop khi Redis down | Option B (clear session + redirect) gây infinite loop: Keycloak SSO session tự re-auth → tạo session mới → Redis vẫn down → clear → redirect → loop | Dùng Option A: trả 500 INTERNAL_SERVER_ERROR trực tiếp, KHÔNG clear session. Session preserved → hồi phục tự động khi Redis về |
| `request.uri.toString()` trả URL nội bộ Docker | Trong OpenIG ScriptableFilter, `request.uri` đã bị rewrite thành backend URL (e.g. `http://wordpress/...`) → redirect gửi browser tới hostname Docker internal | ~~Dùng `request.headers.getFirst('Host')`~~ → FIX-08: dùng pinned `CANONICAL_ORIGIN_APPx` constants (env var + hardcoded fallback). Host header cũng không an toàn vì attacker-controlled |
| Backchannel logout lost khi Redis down | BackchannelLogoutHandler trả 500 (FIX-05) nhưng blacklist key không được ghi. Khi Redis recovery, session vẫn valid (chưa bị revoke) cho đến khi JwtSession hết hạn tự nhiên (30 min) | Production: Redis HA (Sentinel/Cluster) + Keycloak retry config. Lab: ~~accepted limitation~~ **RESOLVED** — appendonly yes enabled (commit 00f5558), blacklist keys survive restart |
| OpenIG 6 config.json không support `${env['']}` | Heap parser gọi `.required()` trước EL evaluation → `${env['VAR']}` trả null → error. Chỉ route files (JSON) mới support EL natively | Dùng `docker-entrypoint.sh` + `sed` thay `__PLACEHOLDER__` trong config.json trước khi OpenIG start. Route files dùng `${env['VAR']}` bình thường |
| `entrypoint.sh` `sed` writes to mounted host file | `docker-entrypoint.sh` chạy `sed -i` trên `/opt/openig/config/config.json`, mà đây chính là file host được bind mount. Sau khi container start, file host bị thay `__PLACEHOLDER__` bằng secret thật → git dirty state | Trước khi git commit: restore `__JWT_SHARED_SECRET__` và `__KEYSTORE_PASSWORD__` trong `config.json`. Kế hoạch fix chuẩn: ghi vào bản copy trong `/tmp` theo K8s init container pattern. Xem thêm: OpenIG `OAuth2ClientFilter` không URL-encode `client_secret` trong POST body, nên secret chỉ được dùng ký tự alphanumeric để tránh Keycloak decode `+` thành space |
| BackchannelLogoutHandler hardcoded RS256 only | Sau khi switch Keycloak clients sang `ES256` ở Phase 1, backchannel `logout_token` được ký bằng `ES256`. Handler fail với `Invalid algorithm: expected RS256, got ES256`, sau đó fail tiếp với `Key with kid=... is not an RSA signing key` | Accept cả `RS256` và `ES256` ở alg check; reconstruct EC key từ JWK `x`/`y` (`P-256`, `ECPublicKeySpec`); dùng `SHA256withECDSA` với `R\|\|S -> DER` conversion cho signature verification |
| OpenIG `OAuth2ClientFilter` không URL-encode `client_secret` | Khi client secret chứa `+`, OpenIG gửi raw value trong `application/x-www-form-urlencoded` body; Keycloak decode `+` thành space → `invalid_client_credentials` dù secret nhìn có vẻ đúng | OIDC client secrets dùng với OpenIG phải là alphanumeric only, không có `+`, `/`, `=`. Nếu đã lỡ rotate secret kiểu Base64, regenerate secret mới và cập nhật đồng bộ Keycloak + `.env` + container runtime. 2026-03-18 confirmed on Stack C Grafana after rotating APP5 to an alphanumeric-only secret and recreating both OpenIG containers |
| `openidentityplatform/openig:latest` (`6.0.2`) is broken in this lab | `openidentityplatform/openig:6.0.1` is the known-good runtime for this project, while `latest=6.0.2` still fails container startup. Do not assume Tomcat major version alone explains compatibility here: `6.0.1` also runs on Tomcat 11 but remains the verified good tag | Pin to `openidentityplatform/openig:6.0.1` in all `docker-compose.yml` files and treat any tag bump as a fresh verification task |
| Stack B clientEndpoint collision (app4) | `02-redmine.json` và `01-jellyfin.json` cùng dùng `clientEndpoint: "/openid/app4"` → OpenIG register callback handler globally, route load sau chiếm handler → Redmine callback dùng Jellyfin client | Đổi Redmine sang `/openid/app3` (xóa dotnet routes không dùng), dùng `SessionBlacklistFilter.groovy` parameterized template với args |
| Direct host port exposure bypasses gateway controls | Legacy app expose thẳng ra host (ví dụ Redmine `3000:3000`) cho phép bypass nginx/OpenIG → bỏ qua toàn bộ SSO/SLO controls | Chỉ expose app qua public URL của gateway. **RESOLVED** - Stack B bỏ `3000:3000` trong Step 5 (`aaf66d5`) |
| ~~Entrypoint `cp -r` không overwrite~~ | ~~`docker-entrypoint.sh` dùng `cp -r /opt/openig /tmp/openig` — khi `/tmp/openig` đã tồn tại (restart), cp tạo subdirectory thay vì overwrite → file cũ vẫn được dùng~~ | **FIXED**: thêm `rm -rf "$DST"` trước `cp -r` trong entrypoint (all 3 stacks). Container restart giờ luôn fresh copy + sed substitution. Production: dùng K8s init container pattern |
| Stack C `X-Forwarded-Proto` gây redirect_uri port mismatch | Thêm `X-Forwarded-Proto $scheme` vào Stack C nginx → OpenIG construct redirect_uri dùng internal Tomcat port (8080) thay vì Host header port (18080) → Keycloak reject `invalid_redirect_uri` | KHÔNG thêm `X-Forwarded-Proto` cho Stack C cho đến khi enable TLS. Stack A/B OK vì port mapping khác (80→80, 9080→80). Chỉ thêm khi chuyển HTTPS |
| Cookie name phải unique per stack | Stack A+C cùng dùng `IG_SSO` + `cookieDomain: ".sso.local"` → cross-stack session collision (decrypt fail hoặc session confusion) | Mỗi stack PHẢI có cookieName riêng: A=`IG_SSO`, B=`IG_SSO_B`, C=`IG_SSO_C` |
| `CANONICAL_ORIGIN_APPx` phải có trên mỗi OpenIG node | Logout redirect, fail-closed redirect, và callback integrity không được dựa vào inbound `Host`. Thiếu env var trên một node làm stack fallback khác nhau và khó debug | Define đầy đủ `CANONICAL_ORIGIN_APP1..APP6` trên từng node. **RESOLVED** - Step 5 bổ sung Stack A/B để đồng bộ với Stack C (`aaf66d5`) |
| Stack C nginx cần giống proxy buffer của A/B | Thiếu `proxy_buffer_size 128k` và `proxy_buffers 4 256k` tạo config drift, dễ gặp lỗi với upstream headers lớn | Giữ Stack C nginx align với A/B. **RESOLVED** - Step 5 thêm buffer config (`aaf66d5`) |
| `Content-Security-Policy` cố ý chưa thêm vào baseline nginx security headers | Grafana, phpMyAdmin, WordPress và các app legacy khác cần CSP khác nhau; một policy dùng chung dễ làm vỡ UI/login hoặc quá permissive nên vô nghĩa | Baseline chỉ gồm `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`. Chỉ thêm CSP sau khi validate từng app; HSTS tiếp tục defer đến khi có TLS |
| Redis RESP parsing: errors ≠ blacklisted | `firstLine != '$-1'` treat Redis errors (`-ERR ...`) as positive blacklist hit → mass false-positive logout | PHẢI check `firstLine.startsWith('$')` trước khi so sánh. Error (`-`) và unexpected response phải throw IOException → fail-closed 500 |
| Vault token cache TOCTOU race | `if (cachedToken == null) { login; globals['vault_token'] = ... }` là check-then-act không atomic → concurrent threads gọi Vault login trùng nhau, waste AppRole uses | Dùng `globals.compute('vault_token') { key, existing -> ... }` — ConcurrentHashMap.compute() đảm bảo atomicity |
| Jellyfin SLO → re-login spinner | Sau SLO, re-login Jellyfin: OAuth2 flow OK nhưng Jellyfin API `/DisplayPreferences/usersettings` trả 403 → web client spinner. Pre-existing, do đặc thù app Jellyfin | `post_logout_redirect_uri` đổi sang `/web/index.html` (skip redirect hop). 403 là Jellyfin app permission, không phải SSO issue |
| Jellyfin logout vẫn phải gọi Keycloak dù thiếu `id_token_hint` | `SloHandlerJellyfin` cũ redirect thẳng về `/web/index.html` nếu session không còn `id_token`, nên browser vẫn giữ session ở [[Keycloak]] | Luôn gọi Keycloak end-session với `client_id` + `post_logout_redirect_uri`; chỉ append `id_token_hint` khi có token. Keycloak 24 vẫn logout được khi thiếu hint |
| Jellyfin `deviceId` không được dựa vào `session.hashCode()` | `session.hashCode()` đổi theo JVM/session lifecycle, không stable qua restart hoặc node khác | Dùng `SHA-256("jellyfin-<sub>")` cắt 32 hex chars trong cả `JellyfinTokenInjector.groovy` và `SloHandlerJellyfin.groovy`; persist `jellyfin_user_sub` để logout rebuild đúng cùng `deviceId` |
| TokenReferenceFilter `token_ref_id` conflict khi nhiều app share cùng `JwtSession` cookie | Stack B/C share một cookie per stack (`IG_SSO_B`, `IG_SSO_C`). Nếu mọi route cùng ghi `session['token_ref_id']`, app sau có thể overwrite Redis pointer của app trước trước khi OAuth callback/logout restore xong | Mỗi app PHẢI bind `tokenRefKey` riêng qua route args (`token_ref_id_app1` .. `token_ref_id_app6`); logout routes phải dùng đúng cùng key với app routes |
| Jellyfin session markers phải giữ nhỏ để không làm phình `JwtSession` | `JellyfinTokenInjector.groovy` lưu `jellyfin_user_sub`, `jellyfin_device_id`, `jellyfin_token` trong cookie-backed session; thêm blob lớn/JSON response sẽ nhanh chóng ăn lại budget 4 KB | Chỉ giữ UUID/short strings; `deviceId` derive từ `SHA-256("jellyfin-<sub>")`; không đưa thêm Jellyfin response/app state vào session |
| JWKS cache race condition (BackchannelLogoutHandler, C-1) | `@Field static volatile` check-then-act trên 2 biến riêng biệt — non-atomic. Thread A đọc cache, Thread B invalidate, Thread A dùng null | Dùng `globals.compute("jwks_cache")` atomic pattern (giống Vault token cache). **RESOLVED** — all 3 stacks dùng template chung sau Pattern Consolidation Step 3, commit `4d8f065` |
| SloHandler thiếu `try/catch` quanh logout flow (H-1) | Stack A và Stack C handlers cũ để exception bubble lên trực tiếp, dễ làm logout path fail không đồng nhất khi config/env/network lỗi | Thêm `try/catch` trong `SloHandler.groovy` template dùng cho all stacks cần pattern chung. **RESOLVED** — Pattern Consolidation Step 4, commit `3b8a6d8` |
| Backchannel logout TTL unit mismatch (H-6, Stack C) | Handler cũ mix seconds/millis khi set Redis blacklist TTL, làm thời gian revoke sai lệch | Chuẩn hóa TTL route arg theo giây và dùng `28800` seconds nhất quán trên all 3 stacks. **RESOLVED** — Pattern Consolidation Step 3, commit `4d8f065` |
| ScriptableHandler không support args? (SAI) | Giả định sai — cả ScriptableHandler và ScriptableFilter đều inherit args từ `AbstractScriptableHeapObject` | Confirmed từ OpenIG 6.0.2 source + runtime smoke test. args = top-level Groovy vars via `binding.hasVariable("key")`. **CẢNH BÁO**: `args as Map` pattern KHÔNG hoạt động — Stack C cũ chạy bằng hardcoded fallback, không phải args. Đã consolidate sang đúng pattern (Step 2) |
| `proxy_cookie_flags` Secure flag NOT added in HTTP lab | Adding `Secure` over HTTP breaks cookies because browsers ignore them on plain HTTP | Dùng `SameSite=Lax` only. Add `Secure` only after TLS enabled. Production: bật `Secure` cùng lúc với TLS termination |
| OpenIG chỉ auto-wire JWT session khi heap object tên đúng là `Session` | Cùng config `type: "JwtSession"` nhưng nếu heap object được đặt tên `"JwtSession"` thay vì `"Session"`, request flow vẫn rơi về Tomcat `HttpSession` và browser chỉ thấy `JSESSIONID` | Muốn dùng cookie-backed session thật sự thì phải rename heap object sang `"Session"` và verify fresh requests set `IG_SSO*`, không phải `JSESSIONID` |
| JwtSession 4KB budget bị đội lên rất nhanh khi cache app cookies trong session | Khi serialized `JwtSession` vượt 4096 chars, OpenIG `SessionFilter.handleResult()` catch `IOException`, log `Failed to save session`, rồi trả downstream response gốc mà không ghi `Set-Cookie` mới. Client vẫn nhận HTTP response bình thường nhưng session state bị silent loss. | Production pattern: browser giữ legacy app cookies, OpenIG session chỉ giữ marker nhỏ (`*_user_sub`, cookie names). TokenReferenceFilter Redis offload ngăn scenario này trong implementation hiện tại; size levers tiếp theo là trim access-token claims, `ES256`, và disable `refresh_token` |
| OAuth2ClientFilter session key includes full app URL, không chỉ `clientEndpoint` path | OpenIG dùng request URL làm một phần session namespace, nên key thực tế có dạng `oauth2:<scheme>://<host>:<port><clientEndpoint>` (ví dụ `oauth2:http://grafana-c.sso.local:18080/openid/app5`) | Dùng `session.keySet().findAll { it.startsWith("oauth2:") }` rồi match endpoint động; không hardcode `oauth2:/openid/appX` |
| Stack C MariaDB/Vault credential drift sau bootstrap | Live MariaDB seed `alice/AlicePass123`, nhưng Vault vẫn giữ password bootstrap cũ cho `secret/phpmyadmin/alice` nên phpMyAdmin SSO fail với MySQL `1045` dù OIDC/AppRole đều OK | Align lại Vault secret theo password MariaDB đang chạy và patch `stack-c/vault/init/vault-bootstrap.sh` để bootstrap lần sau ghi đúng giá trị |
| Stack C phpMyAdmin bob login chưa thể hoạt động | Vault có thể giữ secret `bob`, nhưng live MariaDB chỉ provision `alice`; không có `bob@%` để authenticate | Xử lý như infra gap: tạo user `bob` ở MariaDB theo workflow được duyệt, rồi align Vault; nếu không support thì document rõ alice-only |
| OpenIG non-root blocked by Vault file permissions | `vault/file/openig-role-id` is `-rw-------` (root-only) on macOS host mounts, and `entrypoint.sh` writes `config.json` in place | Lab: keep `user: root` with macOS exception comment. Production: use K8s init container + Vault Agent sidecar, no host mounts |
| IG_SSO* cookies forwarded to backend apps unstripped | No explicit strip logic in nginx or OpenIG routes. All backends under `.sso.local` receive all 3 gateway session cookies. Oversight — stripWpCookies/stripRedmineCookies show intent for app cookies but gateway cookies were not addressed | Strip `IG_SSO\|IG_SSO_B\|IG_SSO_C` from Cookie header in OpenIG route chain AFTER session loaded, BEFORE forwarding. NOT at nginx ingress. Fix tracked as `SEC-COOKIE-STRIP` |
| TokenReferenceFilter restore trên OAuth2 callback phá pending state/nonce | Request callback vẫn chạy restore path nên filter kéo token cũ từ Redis đè lên `oauth2:*` pending state trong shared `JwtSession`, sau đó `OAuth2ClientFilter` fail với `no authorization in progress` | Thêm guard `isOauthCallback = request.uri.path?.contains(configuredClientEndpoint + '/callback')`; nếu true thì bỏ qua Redis restore hoàn toàn |
| `stripOauth2EntriesFromSession` xóa nhầm OAuth2 state của app khác | Hàm gọi `session.clear()` rồi add lại non-`oauth2:*` entries, nên mọi key `oauth2:*` của app khác trong cùng shared `JwtSession` cũng bị xóa; App1 response có thể làm App2 mất nonce/state đang pending | Dùng `discoverOauth2SessionKeys().toSet()` làm `keysToRemove`, chỉ strip OAuth2 namespace của app hiện tại thay vì all `oauth2:*` keys |

## Decision log

| Decision | Lý do |
|----------|-------|
| HA dùng `ip_hash` + JwtSession, không dùng Redis session store | Đã test và confirmed hoạt động. Redis session store phức tạp hơn không cần thiết cho lab. |
| Vault file storage thay vì dev mode | Dev mode mất data khi restart. File storage persist, production-grade. |
| Mỗi stack Redis riêng, không share | Isolation — SLO 1 stack không ảnh hưởng stack khác. Cross-stack SLO qua Keycloak backchannel. |
| Stack B đã thêm `cookieDomain: ".sso.local"` | Đồng bộ với trạng thái hiện tại của dự án, không còn theo dõi như bug. |
| Custom SloHandler thay vì `defaultLogoutGoto` | OpenIG 6.0.2 không có `openIdEndSessionOnLogout` (feature không tồn tại — max version là 6.0.2, không có 6.5+). `defaultLogoutGoto` tĩnh không thể mang `id_token_hint`. | Dùng ScriptableHandler đọc `id_token` từ session key `oauth2:...` |
| phpMyAdmin SLO hoạt động qua 401 | phpMyAdmin `auth_type=http` logout gửi 401 để clear browser Basic Auth → HttpBasicAuthFilter failureHandler trigger → redirect `/openid/app6/logout` → Keycloak end_session | Đây là by design, không cần dedicated logout intercept |
| Jellyfin dùng dedicated Keycloak client | Jellyfin cần `openig-client-b-app4` riêng (không share `openig-client-b` với Redmine) vì namespace OIDC khác nhau | Đảm bảo SloHandler, OAuth2ClientFilter, backchannel logout, post_logout_redirect_uris đều dùng cùng client_id |
| Fail-closed dùng 500 thay vì redirect | Option B (redirect) gây infinite loop do Keycloak SSO re-auth. Option A (500) đơn giản, session preserved, compatible với future K8s + Redis HA. |
| FIX-05: 500 thay vì 400 cho infra errors | Keycloak spec: 400 = bad request (don't retry), 500 = server error (may retry). Trả 500 cho Redis/network errors cho phép Keycloak retry backchannel logout khi infra recovery. |
| FIX-06: docker-entrypoint.sh + sed cho config.json secrets | OpenIG 6 heap parser không support EL trong config.json. Route files hỗ trợ `${env['VAR']}` native. Config.json dùng `__PLACEHOLDER__` + sed substitution tại container startup. sharedSecret + PKCS12 rotated, clientSecret externalized only (must match Keycloak). |
| ES256 requires EC key handling in BackchannelLogoutHandler | Switching Keycloak clients to `ES256` changes both the `alg` field and the key type in JWKS (`kty=EC` instead of `RSA`). Both alg validation and key reconstruction/signature verification must handle EC path. JWS raw `R\|\|S` signature must be DER-encoded before Java `SHA256withECDSA` `Signature.verify()` |
| Pin OpenIG image to `6.0.1` not `latest` | `openig:latest=6.0.2` is still broken in this lab, while `openidentityplatform/openig:6.0.1` is the known-good OpenIG 6 tag. Do not assume Tomcat major version alone determines compatibility: `6.0.1` also runs on Tomcat 11 and still works here |
| Stack B Redmine clientEndpoint `/openid/app3` | Redmine cần namespace riêng, dotnet không dùng nữa → dùng `/openid/app3`. Mỗi app trong cùng OpenIG instance PHẢI có clientEndpoint riêng — collision gây OAuth2 callback bị route sai xử lý |
| Stack B xóa dotnet routes | `01-dotnet.json` + `00-dotnet-logout.json` không dùng nữa, chiếm `/openid/app3` namespace | Xóa để Redmine dùng `/openid/app3` |
| Stack B nginx `hash $cookie_JSESSIONID` broken | OpenIG JwtSession không tạo JSESSIONID → hash empty string → routing không deterministic → OAuth2 state mismatch. Stack A/C dùng `ip_hash` đúng | Đổi sang `ip_hash` cho Stack B (commit 93efa92). Luôn dùng `ip_hash` cho tất cả stacks |
| PhpMyAdminCookieFilter gây Token mismatch | phpMyAdmin `auth_type=http` dùng CSRF token protection liên kết với session cookie. Khi filter manipulate cookie (expire/replace) → CSRF token mismatch → lỗi "Token mismatch" | WONT_FIX — không wire filter vào route chain. User switch handled bởi `cacheHeader: false` + fresh `attributes` per request (FIX-09). **File DELETED** — PhpMyAdminCookieFilter.groovy removed in L-5 fix (Phase 2 STEP-01). |
| OpenIG 6 `HttpBasicAuthFilter` hỗ trợ `${attributes.key}` | Trước đây giả định sai rằng HttpBasicAuthFilter chỉ support `${session['...']}` EL. Source code confirm: `username`/`password` là `Expression<String>` — chấp nhận mọi EL hợp lệ kể cả `${attributes.key}` | Dùng `attributes` (transient per-request) thay `session` (persist vào JwtSession cookie) cho credential injection |
| Pattern Consolidation via `args` | 24 Groovy files chỉ có 7 patterns, 78% duplication (~1676 lines). ScriptableHandler + ScriptableFilter đều support `args` binding (verified từ `AbstractScriptableHeapObject.java`). Consolidate 18 files → 7-8 parameterized templates | Plan: `.omc/plans/pattern-consolidation.md`. Critic ACCEPT sau 1 revision. Step 1 smoke test + Step 2 consolidation đã prove pattern hoạt động. Stack C cũ dùng `args as Map` (SAI) — chạy bằng fallback, không phải args |
