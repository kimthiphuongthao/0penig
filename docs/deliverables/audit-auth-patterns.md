# Auth Pattern Audit — SSO Lab

> Update 2026-03-17: Pattern Consolidation Steps 1-6 are complete. This audit remains primarily historical. STEP-01 deleted `PhpMyAdminCookieFilter.groovy`, STEP-02 rotated Stack C OIDC secrets, and STEP-03 moved compose secrets into gitignored `.env` files while pinning OpenIG to `6.0.1`.

> [!warning]
> Historical snapshot note: several findings below describe the repo state before STEP-01/02/03. Use this file for pattern comparison, not as the latest readiness scoreboard. Operational follow-up 2026-03-18: Stack C Grafana SSO/SLO re-validation passed. The earlier APP5 padding theory was superseded; OpenIG `OAuth2ClientFilter` requires alphanumeric-only clientSecret values because it does not URL-encode `client_secret`.

> Update 2026-03-24: the active runtime topology is a single shared-infra deployment on port `80` with hostname routing through `shared-nginx` to `shared-openig-1` and `shared-openig-2`. Historical `stack-a`/`stack-b`/`stack-c` file references are retained below where they capture the audited pre-consolidation implementation details.

## 1. Per-App Inventory

| App | Stack | Auth Pattern | Session Artifact | Logout Behavior | Proxy Topology | Keycloak Client |
|---|---|---|---|---|---|---|
| WordPress | A | Hybrid: gateway OIDC plus credential injection into WordPress form login (`stack-a/openig_home/config/routes/01-wordpress.json:33-47`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:46-97`) | OpenIG OIDC session plus cached `wp_session_cookies` in session (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:40-41`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:94-97`) | `wp-login.php?action=logout` is intercepted to Keycloak logout; IdP backchannel writes Redis blacklist for `/openid/app1/backchannel_logout` (`stack-a/openig_home/config/routes/00-wp-logout.json:2-8`, `stack-a/openig_home/scripts/groovy/SloHandler.groovy:22-35`, `stack-a/openig_home/config/routes/00-backchannel-logout-app1.json:2-8`) | Browser -> `wp-a.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-wordpress` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:180-198`, `shared/nginx/nginx.conf:22-60`) | `openig-client` (`stack-a/openig_home/config/routes/01-wordpress.json:19-24`) |
| WhoAmI | A | Gateway OIDC plus header-based pre-auth to upstream (`stack-a/openig_home/config/routes/02-app2.json:33-76`); diagnostic utility only (out of scope) | OpenIG OIDC session plus cached `oidc_sid_app2`; no app-local session is evidenced (`stack-a/openig_home/scripts/groovy/SessionBlacklistFilterApp2.groovy:53-80`) | Only OIDC `defaultLogoutGoto` is configured; no app2 logout intercept or app2 backchannel route was found in routes (`stack-a/openig_home/config/routes/02-app2.json:33-47`, `stack-a/openig_home/config/routes/02-app2.json:79-108`) | Browser -> `whoami-a.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-whoami` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:218-223`, `shared/nginx/nginx.conf:62-100`) | `openig-client` (`stack-a/openig_home/config/routes/02-app2.json:19-24`) |
| Redmine | B | Hybrid: gateway OIDC plus Vault-backed credential injection into Redmine form login (`stack-b/openig_home/config/routes/02-redmine.json:18-25`, `stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:27-188`) | OpenIG OIDC session plus cached `redmine_session_cookies` (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:25-28`, `stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:186-191`) | POST `/logout` is intercepted to Keycloak logout; `/openid/app3/backchannel_logout` blacklists the OIDC SID (`shared/openig_home/config/routes/00-redmine-logout.json:1-61`, `shared/openig_home/config/routes/00-backchannel-logout-app3.json:1-24`) | Browser -> `redmine-b.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-redmine`. Historical note: Step 5 removed the earlier direct host exposure on port `3000` (`aaf66d5`; `shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:225-241`, `shared/nginx/nginx.conf:102-156`) | `openig-client-b` (`shared/openig_home/config/routes/02-redmine.json:19-24`) |
| Jellyfin | B | Hybrid: gateway OIDC plus Vault-backed Jellyfin token issuance/injection (`stack-b/openig_home/config/routes/01-jellyfin.json:18-30`, `stack-b/openig_home/scripts/groovy/VaultCredentialFilter.groovy:42-120`, `stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:48-107`) | OpenIG OIDC session plus `jellyfin_token`, `jellyfin_user_id`, `jellyfin_device_id`; token is also written into browser `localStorage` (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:88-107`, `stack-b/openig_home/scripts/groovy/JellyfinResponseRewriter.groovy:48-66`) | `/logout` is intercepted and calls Jellyfin `/Sessions/Logout`; SLO handler reads session key `oauth2:.../openid/app4`, reads env `OIDC_CLIENT_ID_APP4=openig-client-b-app4`, and redirects to Keycloak end-session (FIX-01 resolved — namespace mismatch app3/app4 corrected) (`shared/openig_home/config/routes/00-jellyfin-logout.json:1-57`, `shared/openig_home/config/routes/01-jellyfin.json:19-24`, `shared/openig_home/config/routes/00-backchannel-logout-app4.json:1-24`) | Browser -> `jellyfin-b.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-jellyfin` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:263-272`, `shared/nginx/nginx.conf:158-210`) | `openig-client-b-app4` (`shared/openig_home/config/routes/01-jellyfin.json:19-24`) |
| Grafana | C | Gateway OIDC plus trusted-header auth proxy (`stack-c/openig_home/config/routes/10-grafana.json:33-47`, `stack-c/openig_home/config/routes/10-grafana.json:76-88`, `stack-c/docker-compose.yml:52-62`) | OpenIG OIDC session plus cached `oidc_sid_app5`; `preferred_username` read from transient `attributes.openid` per-request (FIX-09: `grafana_username` removed from session) (`stack-c/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:88-102`, `stack-c/openig_home/config/routes/10-grafana.json:83-86`) | `/logout` is intercepted to Keycloak logout; `/openid/app5/backchannel_logout` blacklists the OIDC SID (`shared/openig_home/config/routes/00-grafana-logout.json:1-61`, `shared/openig_home/config/routes/00-backchannel-logout-app5.json:1-24`) | Browser -> `grafana-c.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-grafana` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:274-288`, `shared/nginx/nginx.conf:212-250`) | `openig-client-c-app5` (`shared/openig_home/config/routes/10-grafana.json:19-24`) |
| phpMyAdmin | C | Hybrid: gateway OIDC plus Vault-backed HTTP Basic injection (`stack-c/openig_home/config/routes/11-phpmyadmin.json:33-47`, `stack-c/openig_home/config/routes/11-phpmyadmin.json:84-97`, `stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:78-157`) | OpenIG OIDC session; `phpmyadmin_username`/`phpmyadmin_password` set in transient `attributes` per-request (FIX-09: removed from session, Vault token cached in `globals`) (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:69-77`, `stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:149-151`) | `/?logout=1` is intercepted to Keycloak logout; `/openid/app6/backchannel_logout` blacklists the OIDC SID (`shared/openig_home/config/routes/00-phpmyadmin-logout.json:1-62`, `shared/openig_home/config/routes/00-backchannel-logout-app6.json:1-24`) | Browser -> `phpmyadmin-c.sso.local` -> `shared-nginx` `:80` -> `shared-openig-1/shared-openig-2` -> `shared-phpmyadmin` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:290-303`, `shared/nginx/nginx.conf:252-290`) | `openig-client-c-app6` (`shared/openig_home/config/routes/11-phpmyadmin.json:19-24`) |

## 2. Control Score Matrix

| App | Auth Trust Boundary | Session Hardening | Logout Coherence | Token/Credential Hygiene | Observability/Audit | Total |
|---|---|---|---|---|---|---|
| WordPress | 2 - OIDC terminates at OpenIG and WordPress is backend-only (`stack-a/openig_home/config/routes/01-wordpress.json:33-47`, `stack-a/docker-compose.yml:101-125`) | 1 - `wp_session_cookies` are cached and cleared on expiry/blacklist, but no cookie-flag or timeout policy is evidenced (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:94-97`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:156-165`, `stack-a/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:84-92`) | 2 - App logout intercept, IdP logout redirect, and backchannel blacklist all exist (`stack-a/openig_home/config/routes/00-wp-logout.json:2-8`, `stack-a/openig_home/scripts/groovy/SloHandler.groovy:22-35`, `stack-a/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:305-322`) | 1 - Vault AppRole and secret lookup exist, but client secret is hardcoded and no revocation policy is implemented (`stack-a/openig_home/scripts/groovy/VaultCredentialFilter.groovy:41-45`, `stack-a/openig_home/scripts/groovy/VaultCredentialFilter.groovy:75-83`, `stack-a/openig_home/config/routes/01-wordpress.json:19-24`) | 1 - Scripts log failures/warnings, but no correlated audit pipeline or alerting is evidenced (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:30-34`, `stack-a/openig_home/scripts/groovy/SloHandler.groovy:32-32`, `stack-a/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:96-97`) | 7 |
| WhoAmI | N/A — diagnostic tool, not scored | N/A — diagnostic tool, not scored | N/A — diagnostic tool, not scored | N/A — diagnostic tool, not scored | N/A — diagnostic tool, not scored | N/A |
| Redmine | ⚠️ 0 - Historical snapshot: Redmine was directly published on host port `3000` at audit time, but current shared-infra keeps `redmine-b.sso.local` behind `shared-nginx` on port `80` (`aaf66d5`, `shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:225-241`, `shared/nginx/nginx.conf:102-156`) | 1 - `_redmine_session` is cached and cleared on redirect to `/login`, and app3 blacklist checks are configured, but no cookie-flag or timeout policy is evidenced (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:177-199`, `shared/openig_home/config/routes/02-redmine.json:87-100`) | 2 - Local logout intercept, `/openid/app3` Keycloak logout, and backchannel blacklist are wired consistently (`shared/openig_home/config/routes/00-redmine-logout.json:1-61`, `shared/openig_home/config/routes/00-backchannel-logout-app3.json:1-24`) | 1 - Vault AppRole flow exists, but client secret is hardcoded and no revocation/blacklist SLA beyond Redis TTL is implemented (`stack-b/openig_home/scripts/groovy/VaultCredentialFilter.groovy:41-45`, `stack-b/openig_home/scripts/groovy/VaultCredentialFilter.groovy:76-85`, `shared/openig_home/config/routes/02-redmine.json:19-24`) | 1 - Login and logout failures are logged, but no correlation IDs or alert rules are evidenced (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:168-188`, `stack-b/openig_home/scripts/groovy/SloHandlerRedmine.groovy:37-42`) | 5 |
| Jellyfin | 2 - Jellyfin is backend-only and OpenIG overwrites `Authorization` before proxying (`shared/docker-compose.yml:263-272`, `shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:148-149`) | 1 - Token/session state is cleared on `401` and app4 blacklist checks exist, but no cookie-flag or timeout policy is evidenced (`shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:118-125`, `shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:151-160`, `shared/openig_home/config/routes/01-jellyfin.json:87-100`) | 2 - App logout intercept, Keycloak end-session redirect with `id_token_hint`, and backchannel blacklist are all wired consistently via `/openid/app4` and `openig-client-b-app4` (FIX-01 resolved) (`shared/openig_home/config/routes/00-jellyfin-logout.json:1-57`, `shared/openig_home/config/routes/01-jellyfin.json:19-24`, `shared/openig_home/config/routes/00-backchannel-logout-app4.json:1-24`) | ⚠️ 0 - Access token is stored server-side and then copied into browser `localStorage` (`shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:138-149`, `shared/openig_home/scripts/groovy/JellyfinResponseRewriter.groovy:29-69`) | 1 - Token/logout failures are logged, but no correlated audit trail is evidenced (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:119-123`, `stack-b/openig_home/scripts/groovy/SloHandlerJellyfin.groovy:55-56`, `stack-b/openig_home/scripts/groovy/SloHandlerJellyfin.groovy:92-103`) | 6 |
| Grafana | 2 - nginx clears `X-WEBAUTH-USER`, OpenIG removes and re-adds it, and Grafana is configured for auth proxy only (`shared/nginx/nginx.conf:234-248`, `shared/openig_home/config/routes/10-grafana.json:147-156`, `shared/docker-compose.yml:274-288`) | 1 - OIDC SID is cached/blacklisted, but no cookie-flag or timeout policy is evidenced (`shared/openig_home/config/routes/10-grafana.json:122-135`) | 2 - Local logout intercept, Keycloak end-session redirect, and app5 backchannel blacklist exist (`shared/openig_home/config/routes/00-grafana-logout.json:1-61`, `shared/openig_home/config/routes/00-backchannel-logout-app5.json:1-24`) | 1 - No downstream bearer token is exposed, but the client secret is hardcoded and there is no revocation/TTL policy beyond OpenIG session state (`shared/openig_home/config/routes/10-grafana.json:19-24`) | 1 - Logout/blacklist warnings exist, but no correlation IDs or alert rules are evidenced (`stack-c/openig_home/scripts/groovy/SloHandlerGrafana.groovy:31-41`, `stack-c/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:152-153`) | 7 |
| phpMyAdmin | 2 - nginx clears `Authorization` and OpenIG injects Basic Auth from gateway session; app is backend-only (`shared/nginx/nginx.conf:274-289`, `shared/openig_home/config/routes/11-phpmyadmin.json:140-156`, `shared/docker-compose.yml:290-303`) | 1 - SID blacklist exists and gateway credentials are purged when the mapped username changes, but no cookie-flag or timeout policy is evidenced (`shared/openig_home/config/routes/11-phpmyadmin.json:93-106`, `stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:69-76`) | 2 - Local `logout=1` intercept, Keycloak logout redirect, and app6 backchannel blacklist exist (`shared/openig_home/config/routes/00-phpmyadmin-logout.json:1-62`, `shared/openig_home/config/routes/00-backchannel-logout-app6.json:1-24`) | 1 - Vault AppRole flow, adjusted token lease, and `cacheHeader=false` exist, but the client secret is hardcoded and no revocation policy is evidenced (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:82-125`, `shared/openig_home/config/routes/11-phpmyadmin.json:19-24`, `shared/openig_home/config/routes/11-phpmyadmin.json:140-156`) | 1 - Vault/logout/blacklist failures are logged, but no correlated audit pipeline is evidenced (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:159-163`, `stack-c/openig_home/scripts/groovy/SloHandlerPhpMyAdmin.groovy:30-40`, `stack-c/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:152-153`) | 7 |

## 3. Audit Questions (Q1-Q10)

### Q1. Login đang xảy ra ở app, gateway hay cả hai?

- WordPress: Cả hai. OpenIG runs OIDC at `/openid/app1`, then `CredentialInjector.groovy` posts credentials to `http://wordpress/wp-login.php` and caches upstream cookies (`stack-a/openig_home/config/routes/01-wordpress.json:33-47`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:46-97`).
- WhoAmI: Gateway only. OpenIG runs OIDC at `/openid/app2` and forwards identity in `X-Authenticated-User`; there is no app login flow in scope (`stack-a/openig_home/config/routes/02-app2.json:33-76`).
- Redmine: Cả hai. OpenIG runs OIDC, then `RedmineCredentialInjector.groovy` does `GET /login` plus `POST /login` to Redmine and caches `_redmine_session` (`stack-b/openig_home/config/routes/02-redmine.json:87-101`, `stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:27-188`).
- Jellyfin: Cả hai. OpenIG runs OIDC, then `JellyfinTokenInjector.groovy` calls `POST /Users/AuthenticateByName` and injects the issued token (`stack-b/openig_home/config/routes/01-jellyfin.json:92-114`, `stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:48-107`).
- Grafana: Gateway only. OpenIG runs OIDC and forwards `X-WEBAUTH-USER` to Grafana auth proxy (`stack-c/openig_home/config/routes/10-grafana.json:33-47`, `stack-c/openig_home/config/routes/10-grafana.json:76-88`, `stack-c/docker-compose.yml:57-61`).
- phpMyAdmin: Cả hai. OpenIG runs OIDC, fetches credentials from Vault, then injects HTTP Basic on requests to phpMyAdmin (`stack-c/openig_home/config/routes/11-phpmyadmin.json:33-47`, `stack-c/openig_home/config/routes/11-phpmyadmin.json:84-97`, `stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:128-157`).

### Q2. Sau login thành công, session ID có đổi không?

- UNVERIFIABLE for all six apps from static code alone. The code proves that WordPress and Redmine expect new `Set-Cookie` values on successful upstream login, but it does not prove whether a pre-auth session ID is rotated versus newly created (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:82-97`, `stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:143-188`).
- For WhoAmI and Grafana, no app-local session cookie is shown in the inspected code; only OpenIG OIDC state is visible (`stack-a/openig_home/config/routes/02-app2.json:33-76`, `stack-c/openig_home/config/routes/10-grafana.json:33-88`).
- For Jellyfin and phpMyAdmin, the code shows token/basic-auth injection, not a before/after browser cookie trace (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:57-107`, `stack-c/openig_home/config/routes/11-phpmyadmin.json:84-97`).

### Q3. User có thể bypass gateway để gọi app trực tiếp không?

- WordPress: No direct host exposure is evidenced in shared-infra; WordPress is backend-only behind `shared-nginx` and `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:180-198`).
- WhoAmI: No direct host exposure is evidenced in shared-infra; WhoAmI is backend-only behind `shared-nginx` and `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:218-223`).
- Redmine: Historical finding only. The old `ports: - "3000:3000"` host exposure was removed in Step 5 [RESOLVED 2026-03-17] (`aaf66d5`), and current browser ingress stays behind `shared-nginx` on port `80` with hostname routing to `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:225-241`, `shared/nginx/nginx.conf:102-156`).
- Jellyfin: No direct host exposure is evidenced in shared-infra; Jellyfin is backend-only behind `shared-nginx` and `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:263-272`).
- Grafana: No direct host exposure is evidenced in shared-infra; Grafana is backend-only behind `shared-nginx` and `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:274-288`).
- phpMyAdmin: No direct host exposure is evidenced in shared-infra; phpMyAdmin is backend-only behind `shared-nginx` and `shared-openig-*` (`shared/docker-compose.yml:91-140`, `shared/docker-compose.yml:290-303`).

### Q4. App có tin tưởng `X-Forwarded-*`/`X-Remote-User` từ client không?

- WordPress: No trusted identity-header mode is evidenced; OpenIG injects cookies instead (`stack-a/openig_home/config/routes/01-wordpress.json:98-103`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:120-127`).
- WhoAmI: WhoAmI excluded — diagnostic utility.
- Redmine: No trusted identity-header mode is evidenced; OpenIG injects Redmine cookies instead (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:191-191`).
- Jellyfin: Not as a trusted upstream identity header. OpenIG overwrites `Authorization` before proxying (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:105-106`).
- Grafana: No. `shared-nginx` clears `X-WEBAUTH-USER`, and OpenIG removes and re-adds it before proxying (`shared/nginx/nginx.conf:234-248`, `shared/openig_home/config/routes/10-grafana.json:147-156`).
- phpMyAdmin: No. `shared-nginx` clears `Authorization`, and OpenIG injects HTTP Basic from gateway session (`shared/nginx/nginx.conf:274-289`, `shared/openig_home/config/routes/11-phpmyadmin.json:140-156`).

### Q5. Logout từ app có làm mất session tại IdP/gateway không?

- WordPress: Yes. App logout is intercepted, `session.clear()` runs, and the browser is redirected to Keycloak end-session (`stack-a/openig_home/config/routes/00-wp-logout.json:2-8`, `stack-a/openig_home/scripts/groovy/SloHandler.groovy:22-35`).
- WhoAmI: WhoAmI excluded — diagnostic utility.
- Redmine: Yes. POST `/logout` is intercepted, OpenIG clears session state, and redirects to Keycloak logout through `/openid/app3` (`shared/openig_home/config/routes/00-redmine-logout.json:1-61`).
- Jellyfin: Yes. `/logout` clears Jellyfin server session via `/Sessions/Logout`, clears the OpenIG session, and redirects to Keycloak end-session with `id_token_hint` using `/openid/app4` and `OIDC_CLIENT_ID_APP4=openig-client-b-app4` (FIX-01 resolved) (`shared/openig_home/config/routes/00-jellyfin-logout.json:1-57`, `shared/openig_home/config/routes/01-jellyfin.json:19-24`).
- Grafana: Yes. `/logout` is intercepted, OpenIG clears session state, and redirects to Keycloak logout through `/openid/app5` (`shared/openig_home/config/routes/00-grafana-logout.json:1-61`).
- phpMyAdmin: Yes. `/?logout=1` is intercepted, OpenIG clears session state, and redirects to Keycloak logout through `/openid/app6` (`shared/openig_home/config/routes/00-phpmyadmin-logout.json:1-62`).

### Q6. Logout từ IdP có invalidate local session app không?

- WordPress: Yes for the gateway-mediated SSO session. Backchannel logout performs full RS256 JWT validation then stores the SID in Redis, and `SessionBlacklistFilter.groovy` clears session state on the next request (`stack-a/openig_home/config/routes/00-backchannel-logout-app1.json:2-8`, `stack-a/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:305-322`, `stack-a/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:69-92`).
- WhoAmI: WhoAmI excluded — diagnostic utility.
- Redmine: Yes for the gateway-mediated SSO session. Backchannel logout performs full RS256 JWT validation then blacklists the SID via `/openid/app3/backchannel_logout`, and the shared blacklist filter clears session state on the next request (`shared/openig_home/config/routes/00-backchannel-logout-app3.json:1-24`, `stack-b/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:312-329`, `shared/openig_home/config/routes/02-redmine.json:87-100`).
- Jellyfin: Yes for the gateway-mediated SSO session. Backchannel logout blacklists the SID via `/openid/app4/backchannel_logout`, and the shared blacklist filter clears session state on the next request (FIX-01 resolved — namespace now consistent with route and blacklist filter) (`shared/openig_home/config/routes/00-backchannel-logout-app4.json:1-24`, `stack-b/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:305-329`, `shared/openig_home/config/routes/01-jellyfin.json:87-100`).
- Grafana: Yes for the gateway-mediated SSO session. Backchannel logout performs full RS256 JWT validation then blacklists the SID, and the shared blacklist filter clears session state (`shared/openig_home/config/routes/00-backchannel-logout-app5.json:1-24`, `stack-c/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:217-321`, `shared/openig_home/config/routes/10-grafana.json:122-135`).
- phpMyAdmin: Yes for the gateway-mediated SSO session. Backchannel logout performs full RS256 JWT validation then blacklists the SID, and the shared blacklist filter clears session state on the next request (`shared/openig_home/config/routes/00-backchannel-logout-app6.json:1-24`, `stack-c/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:217-321`, `shared/openig_home/config/routes/11-phpmyadmin.json:93-106`).

### Q7. Token/credential có bị log lộ ở reverse proxy/app logs không?

- UNVERIFIABLE for runtime reverse-proxy/app logs from static code alone.
- Within the inspected OpenIG/Groovy code, I did not find explicit statements that log passwords, bearer tokens, or Vault tokens. The log statements in scope log statuses and generic failures instead, for example WordPress (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:47-47`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:105-111`), Redmine (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:168-179`), Grafana (`stack-c/openig_home/scripts/groovy/SloHandlerGrafana.groovy:34-41`), and phpMyAdmin (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:159-163`).
- Separate from logging, Jellyfin does expose its downstream access token to the browser by writing it into `localStorage`, which is a credential-leakage issue even though it is not a log issue (`stack-b/openig_home/scripts/groovy/JellyfinResponseRewriter.groovy:51-66`).

### Q8. Có policy revoke/blacklist cho token và propagation SLA không?

- WordPress: Partial. The backchannel handler performs full RS256 JWT validation (signature via JWKS, iss, aud=`openig-client`, events, iat, exp, sid/sub) before writing a Redis blacklist entry with TTL `EX 1800`, aligned to the current `JwtSession` timeout (`30 minutes`), but no token revocation call to Keycloak or another AS endpoint is made (`stack-a/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:218-322`; current TTL alignment commit 9cbf71a).
- WhoAmI: WhoAmI excluded — diagnostic utility.
- Redmine: Partial. The backchannel handler performs full RS256 JWT validation (signature via JWKS, iss, aud=`openig-client-b`, events, iat, exp, sid/sub) before writing to Redis with TTL `EX 1800`, aligned to the current `JwtSession` timeout; no upstream token revocation call or propagation SLA beyond request-time Redis checks (`stack-b/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:225-329`, `shared/openig_home/config/routes/00-backchannel-logout-app3.json:1-24`, `shared/openig_home/config/routes/02-redmine.json:87-100`; current TTL alignment commit 9cbf71a).
- Jellyfin: Partial. The backchannel handler performs full RS256 JWT validation before writing to Redis with TTL `EX 1800`, aligned to the current `JwtSession` timeout; namespace mismatch is resolved (FIX-01, commit a3cb6c3), and blacklist propagation is consistent with the protected route (`stack-b/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:225-329`, `shared/openig_home/config/routes/00-backchannel-logout-app4.json:1-24`; current TTL alignment commit 9cbf71a).
- Grafana: Partial. The backchannel handler performs full RS256 JWT validation (signature via JWKS, iss, aud=`openig-client-c-app5`, events, iat, exp, sid/sub) before writing to Redis with TTL `EX 1800`, aligned to the current `JwtSession` timeout; no explicit token revocation call or propagation SLA beyond request-time Redis checks (`stack-c/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:217-321`, `shared/openig_home/config/routes/00-backchannel-logout-app5.json:1-24`, `shared/openig_home/config/routes/10-grafana.json:122-135`; current TTL alignment commit 9cbf71a).
- phpMyAdmin: Partial. The backchannel handler performs full RS256 JWT validation (signature via JWKS, iss, aud=`openig-client-c-app6`, events, iat, exp, sid/sub) before writing to Redis with TTL `EX 1800`, aligned to the current `JwtSession` timeout; no explicit token revocation call (`stack-c/openig_home/scripts/groovy/BackchannelLogoutHandler.groovy:217-321`, `shared/openig_home/config/routes/00-backchannel-logout-app6.json:1-24`, `shared/openig_home/config/routes/11-phpmyadmin.json:93-106`; current TTL alignment commit 9cbf71a).

### Q9. LDAP auth có dùng TLS và giới hạn bind permissions không?

- Not applicable in the inspected scope. I found no LDAP/LDAPS/StartTLS configuration or LDAP bind logic in the reviewed gateway files; the scoped apps use OIDC, trusted headers, Vault lookups, form login, or HTTP Basic instead (`stack-a/openig_home/config/routes/01-wordpress.json:33-47`, `shared/openig_home/config/routes/02-redmine.json:69-140`, `shared/openig_home/config/routes/11-phpmyadmin.json:117-176`).

### Q10. Có rate limit/lockout cho login endpoint không?

- No evidence for any scoped app. The active shared nginx config does not contain request-rate limiting directives, and none of the inspected OpenIG routes/scripts implement lockout or throttling (`shared/nginx/nginx.conf:5-328`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:46-97`, `shared/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:179-220`, `shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:90-141`, `shared/openig_home/config/routes/11-phpmyadmin.json:117-176`).
- This is especially relevant for WordPress, Redmine, Jellyfin, and phpMyAdmin because OpenIG is automatically driving upstream credentialed login/token issuance flows (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:46-97`, `shared/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:179-220`, `shared/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:90-141`, `shared/openig_home/config/routes/11-phpmyadmin.json:117-176`).

## 4. Gap Summary

| App | Domain | Score | Gap Description | Suggested Fix (OpenIG-side only) |
|---|---|---:|---|---|
| Redmine | Auth Trust Boundary | 0 (historical audit snapshot) | Redmine was directly exposed on host port `3000` at audit time, bypassing nginx/OpenIG. Step 5 removed that host publication [RESOLVED 2026-03-17] (`aaf66d5`), and current ingress is the shared-infra hostname route `redmine-b.sso.local` on port `80`. | Completed in Step 5 [RESOLVED 2026-03-17]: browser ingress now stays behind `shared-nginx` and `shared-openig-*` on port `80`. |
| Jellyfin | Token/Credential Hygiene | 0 | Jellyfin access token is copied into browser `localStorage` by response rewriting (`stack-b/openig_home/scripts/groovy/JellyfinResponseRewriter.groovy:51-66`). | Keep the downstream token server-side in OpenIG only; remove browser-side token injection and proxy authenticated API calls at the gateway. |
| WordPress | Session Hardening | 1 | Upstream cookies are cached and cleared on expiry, but there is no evidenced gateway session timeout or cookie hardening policy (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:94-97`, `stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:156-165`). | Add explicit OpenIG/nginx session-cookie hardening and timeout settings, and clear cached upstream cookies on every logout path. |
| WordPress | Token/Credential Hygiene | 1 | Vault token caching exists, but the OIDC client secret is hardcoded in route JSON and there is no revocation path (`stack-a/openig_home/config/routes/01-wordpress.json:19-24`, `stack-a/openig_home/scripts/groovy/VaultCredentialFilter.groovy:75-83`). | Move client secrets to Vault/file-backed env injection and add gateway-side revocation/blacklist handling for logout flows. |
| WordPress | Observability/Audit | 1 | Only local script logs are present; there is no correlation ID or central auth/logout audit evidence (`stack-a/openig_home/scripts/groovy/CredentialInjector.groovy:105-111`, `stack-a/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:96-97`). | Add gateway request IDs and structured auth/logout log fields in Groovy/nginx for success, failure, logout, and blacklist decisions. |
| Redmine | Session Hardening | 1 | `_redmine_session` is cached and cleared on redirect-to-login, but no gateway cookie hardening or timeout policy is evidenced (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:177-199`). | Add explicit OpenIG session timeout/cookie hardening and clear cached upstream cookies on all logout/backchannel paths. |
| Redmine | Token/Credential Hygiene | 1 | Vault AppRole token reuse exists, but the OIDC client secret is hardcoded and there is no token revocation path (`stack-b/openig_home/config/routes/02-redmine.json:18-25`, `stack-b/openig_home/scripts/groovy/VaultCredentialFilter.groovy:76-85`). | Move client secrets to Vault/file-backed env and add explicit gateway-side revocation/blacklist handling documentation and code paths. |
| Redmine | Observability/Audit | 1 | Login/logout errors are logged locally, but there is no correlated audit stream (`stack-b/openig_home/scripts/groovy/RedmineCredentialInjector.groovy:168-179`, `stack-b/openig_home/scripts/groovy/SloHandlerRedmine.groovy:37-42`). | Add structured request/session/user fields to gateway logs and emit explicit auth/logout success/failure records. |
| Jellyfin | Session Hardening | 1 | Jellyfin token/session state is cleared on `401`, but no timeout/cookie policy is evidenced (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:76-82`, `stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:108-115`). | Add gateway-side session timeout/cookie hardening and avoid long-lived downstream token reuse without expiry checks. |
| Jellyfin | Logout Coherence | 2 | **RESOLVED** — logout/backchannel wiring is normalized to `/openid/app4` with dedicated client `openig-client-b-app4`; the earlier `/openid/app3` mismatch was fixed in FIX-01 (commit a3cb6c3) and preserved through the Step 4 SloHandler consolidation (`3b8a6d8`). | Keep the dedicated Jellyfin client/endpoint pairing intact when extending the shared-infra Jellyfin route or its Keycloak client. |
| Jellyfin | Observability/Audit | 1 | There are local error logs but no correlated audit trail for token issuance/logout propagation (`stack-b/openig_home/scripts/groovy/JellyfinTokenInjector.groovy:119-123`, `stack-b/openig_home/scripts/groovy/SloHandlerJellyfin.groovy:92-103`). | Emit structured gateway events for token issuance, logout forwarding, backchannel receipt, and blacklist hits. |
| Grafana | Session Hardening | 1 | SID blacklist and username caching exist, but no gateway session timeout/cookie hardening is evidenced (`stack-c/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:95-120`). | Add explicit OpenIG session timeout/cookie hardening and retain only minimal header-mapping state. |
| Grafana | Token/Credential Hygiene | 1 | The trusted-header pattern avoids downstream bearer tokens, but the OIDC client secret is hardcoded and no revocation policy is evidenced (`stack-c/openig_home/config/routes/10-grafana.json:19-24`). | Move the client secret to Vault/file-backed env and add explicit gateway-side revocation/blacklist policy documentation. |
| Grafana | Observability/Audit | 1 | Only local logout/blacklist logs are present (`stack-c/openig_home/scripts/groovy/SloHandlerGrafana.groovy:31-41`, `stack-c/openig_home/scripts/groovy/SessionBlacklistFilter.groovy:152-153`). | Add structured gateway auth/logout logs with request ID, username, session key, and blacklist result. |
| phpMyAdmin | Session Hardening | 1 | SID blacklist exists, but no session timeout/cookie-flag policy is evidenced in the active shared route chain (`shared/openig_home/config/routes/11-phpmyadmin.json:93-106`). | Add explicit OpenIG session timeout/cookie hardening in the shared phpMyAdmin route if browser cookie hygiene is required. |
| phpMyAdmin | Token/Credential Hygiene | 1 | Vault token lease adjustment and `cacheHeader=false` exist, but the OIDC client secret is hardcoded and no revocation path is evidenced (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:122-157`, `stack-c/openig_home/config/routes/11-phpmyadmin.json:19-24`, `stack-c/openig_home/config/routes/11-phpmyadmin.json:84-89`). | Move client secrets to Vault/file-backed env and add explicit gateway-side revocation/blacklist policy for logout events. |
| phpMyAdmin | Observability/Audit | 1 | Only local Vault/logout/blacklist logs are present (`stack-c/openig_home/scripts/groovy/VaultCredentialFilter.groovy:159-163`, `stack-c/openig_home/scripts/groovy/SloHandlerPhpMyAdmin.groovy:30-40`). | Add structured gateway auth/logout logs with request ID, user mapping, and blacklist outcome. |

## 5. Overall Score

- Total score (excluding WhoAmI diagnostic utility): `32 / 50`
- Per-app totals (scored): WordPress `7`, Redmine `5`, Jellyfin `6`, Grafana `7`, phpMyAdmin `7`
- Interpretation tier from reference doc: `UNDEFINED / UNVERIFIABLE`. `docs/legacy-auth-patterns-definitive.md` defines per-control scores `0/1/2` (`docs/legacy-auth-patterns-definitive.md:290-298`) but does not define aggregate score bands or tier names.

Verification:

- All 6 scoped apps appear in the inventory table and the control matrix.
- All 5 control domains are scored for every app.
- All 10 audit questions are answered.
- Suggested fixes are limited to gateway-side changes: OpenIG routes/Groovy, nginx, secret handling, and deployment ingress; no target app code/native app config changes are proposed.
